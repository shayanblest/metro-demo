import { AdminLevelDirective } from './admin-level.directive';

describe('AdminLevelDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminLevelDirective();
    expect(directive).toBeTruthy();
  });
});
