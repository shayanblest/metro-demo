import { PriceSeparatorPipe } from './price-separator.pipe';

describe('PriceSeparatorPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceSeparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
