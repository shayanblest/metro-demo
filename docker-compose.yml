version: '3.8'
services:
  angular:
    build: .
    ports:
      - "4200:80"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    restart: unless-stopped
