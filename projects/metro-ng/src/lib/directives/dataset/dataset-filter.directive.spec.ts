import { DatasetFilterDirective } from './dataset-filter.directive';

describe('DatasetFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new DatasetFilterDirective();
    expect(directive).toBeTruthy();
  });
});
